# TODO

- [ ] Create edit plan: swap left/right blocks in `src/components/Hero.jsx` so image on left and "PAW VASQUEZ" text on right.
- [ ] Apply code change in `src/components/Hero.jsx` (swap JSX blocks / adjust comments).
- [ ] Run `npm test`/`npm run build` or at least `npm run dev` check for errors.
